name = "smartstudy-typing-ai"
main = "typing-sentence-worker.js"
compatibility_date = "2024-01-01"

# secrets (API key না, তাই এখানে না — টার্মিনাল থেকে বসাও):
#   npx wrangler secret put GROQ_KEYS
#   npx wrangler secret put MISTRAL_KEYS
#   npx wrangler secret put GEMINI_KEYS
#   npx wrangler secret put OPENROUTER_KEYS
#   npx wrangler secret put CEREBRAS_KEYS
#   npx wrangler secret put TOGETHER_KEYS
#   npx wrangler secret put FIREWORKS_KEYS
#   npx wrangler secret put DEEPSEEK_KEYS
#   npx wrangler secret put APP_SHARED_SECRET
#
# মান হিসেবে GitHub Secrets-এ যা আছে ঠিক সেই একই comma-separated key-list বসাও —
# একই provider pool দুই জায়গায় (batch + real-time) reuse হচ্ছে।
